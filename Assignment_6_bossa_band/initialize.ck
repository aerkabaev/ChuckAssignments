//Assignment_6_AE_bossa_nova

// It is very difficult to compose jazz within natural minor
// without major dominant chord (69 midi note).
// So some bossa-nova

// Sounds like D'n'B, but I tried to compose a bossa nova

// add score.ck
me.dir() + "/score.ck" => string scorePath;
Machine.add(scorePath);


